// once converted to executable, it can't read from .env so just using a simple config object instead
export const config = {
  PATH_TO_QUES: "C:\\path\\to\\ques",
  PATH_TO_RECORDER: "C:\\path\\to\\recorder",
};
